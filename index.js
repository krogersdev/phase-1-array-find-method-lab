function superbowlWin(data) {
    console.log(data)
    let result = data.find((element) => element.result === 'W')
    console.log(result)
        
    if (result) {
    
    return (result.year);}
}

